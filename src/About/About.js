import React from 'react';
import Header from '../Common/Header';

function About() {
    const aboutHeader = "This is all about drinking!!!";
    return (
        <Header headerText={aboutHeader} />
    );
}

export default About;